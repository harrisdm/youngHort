<h1>Ambassador#show</h1>
<p>Find me in app/views/ambassador/show.html.erb</p>


<h1>Ambassadors</h1>
<p>Below are a list of ambassadors submitted</p>

<div class="table">
  <table>
  <tr>
    <td><%= @ambassador.name %></td> 
    <td><%= @ambassador.location %></td> 
    <td><%= @ambassador.context %></td>
    <td><%= image_tag(@ambassador.image) %></td>   

  </tr>
</table>
</div>

<p>
 <a href="/ambassadors/<%= @ambassador.id %>/edit">Edit</a>
 <%=  link_to( 'Delete', ambassador_path, :method => :delete, data: {confirm: "Are you sure you want to delete this ambassador"}) %>
</p>
